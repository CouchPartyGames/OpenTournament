namespace OpenTournament.Tests.Unit;

using OpenTournament.Common.Draw.Layout;

public class SingleEliminationDrawTest
{

    [Fact(Skip = "Not Ready")]
    public void GetMatchesInRound_Should_When()
    {
        /*
        //Arrange
        var expected = '';
        
        DrawSize size = DrawSize.Create(DrawSize.Size.Size8); 
        
        // Act
        var draw = new SingleEliminationDraw(positions, size);
        var firstRound = draw.GetMatchesInRound(1);

        // Assert

        */
    }
}
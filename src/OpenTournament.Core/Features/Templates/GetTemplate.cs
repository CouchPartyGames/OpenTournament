namespace OpenTournament.Core.Features.Templates;

public static class GetTemplate
{
    /*
   public record GetTemplateQuery();
   
   public static void MapEndpoint(this IEndpointRouteBuilder app) =>
       app.MapGet("templates/{id}", (string id, GetTemplateQuery query, CancellationToken token)
               =>
           {
               return Endpoint(id, query, token);
           })
           .WithTags("Template")
           .WithSummary("Get Template")
           .WithDescription("Get a Tournament Template")
           .WithOpenApi();
   public static async Task<Results<Ok, NotFound>> Endpoint(string id, GetTemplateQuery query, CancellationToken token)
   {
       return TypedResults.Ok();
   }
   */
}
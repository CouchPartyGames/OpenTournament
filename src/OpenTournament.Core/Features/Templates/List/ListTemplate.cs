namespace OpenTournament.Core.Features.Templates.List;

public static class ListTemplate
{
    /*
    public record ListTemplateQuery();

    public static void MapEndpoint(this IEndpointRouteBuilder app)
    {
        app.MapGet("/templates", (string id, CancellationToken token) =>
        {
            return Endpoint(id, token);
        })
        .WithTags("Template")
        .WithSummary("List Template")
        .WithDescription("List Available Templates")
        .WithOpenApi();
    }

    public static async Task<Results<NoContent, NotFound>> Endpoint(string id, CancellationToken token)
    {
        return TypedResults.NoContent();
    }
    */
}
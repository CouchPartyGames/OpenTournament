namespace OpenTournament.Core.Features.Authentication;

public static class Register
{

    /*
    public static void Register(this IEndpointRouteBuilder app) =>
        app.MapPost("/register", Endpoint)
            .WithTags("Authentication", "Register")
            .WithSummary("Register")
            .WithDescription("Register a new user")
            .WithOpenApi();


    public static async Task<Results<Ok, BadRequest>> Endpoint()
    {

        return TypedResults.Ok();
    }
    */
}